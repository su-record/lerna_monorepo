/**
 * { key = value } 가 모두 string 인 상수만 작성
 * */

export const enum LOCALE_CODE {
  KO = 'ko',
  EN = 'en',
  ZH = 'zh',
  JA = 'ja',
}

export const enum STRING_CHECKED {
  TRUE = 'Y',
  FALSE = 'N',
}

export const enum DIALOG_ACTIONS {
  ALERT = 'alert',
  CONFIRM = 'confirm',
}
